#include <stdio.h>

int main()
{

    int a = 22;
    int b = a;
    int c = b * 2;
    int d = 1, e;

    int oldAge = 22;
    int years = 2;
    int newAge = oldAge + years;

    int x, y, z;
    x = y = x = 3;

    return 0;
}